#include<stdio.h>
#include<math.h>
main(){
	float raio, area;
	
	printf("insira o valor de raio: ");
	scanf("%f", &raio);
	area = (3.14 * raio) * raio;
	printf("A area e igual a %.1f\n", area);
	return 0;
}
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <locale.h>
#include <math.h>

main (){
	float bturesidencial, btucomercial, incsol, btup;
	float pessoas, totalpessoal, aparelhos;
	float area, largura, comprimento;

	char regiao[3];
	regiao[1] = 100
	regiao[2] = 200
	regiao[3] = 300

	char tipoMoradia[2];
	tipoMoradia[1] = Casa
	tipoMoradia[2] = Apartamento

	area = largura * comprimento
	totalpessoal = pessoas - 1
	btup = 600
	btucomercial = btup + 200
	bturesidencial = btup
	
	printf("Qual a região em que está? ");
	scanf("%f", &regiao);

	switch{
		case (regiao == 1){
			printf("Apartamento ou Casa? ");
			scanf("%f", &tipoMoradia)
			if()
		}
	}
}